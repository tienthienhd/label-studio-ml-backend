version: "3.8"

services:
  ml-backend:
    platform: linux/amd64
    container_name: tabel-cell-backend
    image: registry-dev.tcgroup.vn/lstdml_tabel_cell_backend
    build: .
    environment:
      - MODEL_DIR=/data/models
      - WORKERS=2
      - THREADS=4
      - LOG_LEVEL=DEBUG
      - ANY=PARAMETER
    ports:
      - "9091:9090"
